# FRAA 三章节修改前后中英对照稿

本文档用于人工精修，不是投稿源文件。内容覆盖 `Introduction`、`Related Work`、`Limitations and Reproducibility` 三章。每章均包含四部分：

1. 修改前英文原文
2. 修改前中文翻译
3. 建议修改后英文稿
4. 建议修改后中文翻译

源文件：`figure_workspace_0ee7a6fe-ccd3-4659-a621-c986942de0c7/submission_pricai2026.tex`

## 1. Introduction

### 1.1 修改前英文原文

```latex
\section{Introduction}

Financial institutions assess user-level risk from evidence that is heterogeneous, time-varying, and often incomplete at the moment of decision. Such evidence includes transaction histories, account states, macroeconomic indicators, regulatory disclosures, and news sentiment. Reliable risk estimates support credit review, fraud prevention, margin control, and compliance workflows \cite{hand1997statistical,thomas2000survey,crook2007recent,bolton2002statistical,ngai2011application}. In practice, many deployed systems still use carefully engineered tabular features with tree-based models such as Random Forests and gradient boosting \cite{breiman2001random,friedman2001greedy,chen2016xgboost,lessmann2015benchmarking}. These models remain strong operational baselines. However, their predictive state is usually fixed by the feature store, and they do not naturally incorporate long behavioral histories or newly released external information.

Neural sequence models and Transformer architectures offer a more flexible way to model user behavior over time \cite{hochreiter1997lstm,cho2014learning,vaswani2017attention,kang2018sasrec,sun2019bert4rec,jurgovsky2018sequence}. Yet a purely parametric sequence model remains limited in a financial risk setting. It can use market or regulatory information only if that information was encoded during training or manually attached as a feature. It also gives reviewers little direct evidence for why a high-risk case was flagged. Retrieval-augmented generation (RAG) and dense retrieval methods provide a way to condition decisions on external evidence available at inference time \cite{robertson2009probabilistic,karpukhin2020dpr,lewis2020rag,izacard2021leveraging}. In parallel, work on explanation and calibration has shown that model outputs need to be inspectable as well as accurate \cite{ribeiro2016lime,lundberg2017shap,guidotti2018survey,arrieta2020explainable,guo2017calibration,niculescu2005predicting,zadrozny2002transforming}. These observations motivate a risk model that retrieves time-valid financial evidence and links that evidence to both prediction and explanation.

This work develops the \textbf{Financial Risk Assessment Agent (FRAA)}, a retrieval-augmented model for explainable financial risk assessment. The word ``agent'' is used in a constrained operational sense. For each scoring request, FRAA builds a user state, queries a timestamped financial knowledge base, fuses retrieved evidence with behavioral features, estimates a risk probability, and generates a short explanation grounded in the retrieved context and influential feature groups. FRAA is therefore a structured retrieve-fuse-score-explain pipeline rather than an open-ended autonomous system.

The main contributions are:
\begin{itemize}
    \item We design a retrieval-augmented financial risk model that combines time-aware sequence encoding, external evidence retrieval, cross-attention evidence fusion, calibrated risk scoring, and explanation generation.
    \item We evaluate FRAA on a large-scale proprietary multi-source dataset with a strict chronological split, and compare it with deployed tree-based baselines and neural sequence models.
    \item We report component ablations, feature-group occlusion, retrieval-depth analysis, scenario adaptation, explanation quality assessment, and latency measurements to examine the contribution and operational cost of the retrieval and explanation components.
\end{itemize}
```

### 1.2 修改前中文翻译

金融机构需要根据异构、时变且在决策时往往不完整的证据来评估用户级风险。这些证据包括交易历史、账户状态、宏观经济指标、监管披露和新闻情绪。可靠的风险估计能够支持信贷审核、欺诈防控、保证金管理和合规流程。在实际部署中，许多系统仍然使用精心设计的表格特征，并配合随机森林、梯度提升等树模型。这些模型仍然是很强的业务基线。然而，它们的预测状态通常受限于特征库，难以自然地纳入较长的行为历史或训练后新出现的外部信息。

神经序列模型和 Transformer 架构为建模用户随时间变化的行为提供了更灵活的方式。然而，在金融风险场景中，纯参数化序列模型仍然存在局限。市场或监管信息只有在训练时已被编码，或者被人工附加为特征时，才能被模型使用。同时，这类模型也难以向审核人员直接说明为什么某个高风险案例被标记。检索增强生成和稠密检索方法提供了一种在推理时基于外部证据进行决策的方式。与此同时，可解释性和校准研究表明，模型输出不仅需要准确，也需要可审查。因此，需要一种能够检索时间有效金融证据，并将证据同时关联到预测和解释的风险模型。

本文提出 \textbf{Financial Risk Assessment Agent (FRAA)}，一种用于可解释金融风险评估的检索增强模型。这里的 “agent” 是受限业务意义上的 agent。对于每个评分请求，FRAA 构建用户状态，查询带时间戳的金融知识库，将检索证据与行为特征融合，估计风险概率，并生成基于检索上下文和重要特征组的简短解释。因此，FRAA 是一个结构化的“检索、融合、评分、解释”流程，而不是开放式自主系统。

本文主要贡献如下：

- 设计了一种检索增强金融风险模型，结合时间感知序列编码、外部证据检索、交叉注意力证据融合、校准风险评分和解释生成。
- 在一个大规模专有多源数据集上，使用严格时间划分评估 FRAA，并与已部署树模型基线和神经序列模型进行比较。
- 报告组件消融、特征组遮蔽、检索深度分析、场景迁移、解释质量评估和延迟测试，以分析检索与解释组件的贡献和业务成本。

### 1.3 建议修改后英文稿

```latex
\section{Introduction}

Financial institutions increasingly make risk decisions from evidence that changes across time and across information sources. A user-level risk assessment may depend on recent transactions, account states, product holdings, macroeconomic signals, regulatory disclosures, and market news. Reliable estimates are important not only for credit review and fraud prevention, but also for margin control, compliance workflows, and analyst triage \cite{hand1997statistical,thomas2000survey,crook2007recent,bolton2002statistical,ngai2011application}. In these settings, a useful model should provide calibrated probabilities, identify high-risk cases for review, and expose enough evidence for human inspection.

This requirement creates three practical challenges. First, financial behavior is temporal and irregular: a short burst of transactions, a delayed repayment, or a sudden balance change may carry different implications depending on when it occurs. Second, risk-relevant evidence is not limited to the internal feature store. News sentiment, regulatory language, and macroeconomic commentary can change after model training, yet still affect the meaning of a current case. Third, the output must be auditable. A probability alone is often insufficient in a regulated review process, because analysts need to understand which evidence and feature groups support the score.

Existing approaches address only part of this problem. Tree ensembles and gradient boosting models remain strong operational baselines for heterogeneous tabular features \cite{breiman2001random,friedman2001greedy,chen2016xgboost,lessmann2015benchmarking}, but their predictive state is largely constrained by manually maintained feature stores. Neural sequence models and Transformer architectures can capture evolving user behavior \cite{hochreiter1997lstm,cho2014learning,vaswani2017attention,kang2018sasrec,sun2019bert4rec,jurgovsky2018sequence}, but a purely parametric model can only use external evidence if it was encoded during training or manually attached as an input. Retrieval-augmented generation and dense retrieval methods can condition outputs on non-parametric evidence at inference time \cite{robertson2009probabilistic,karpukhin2020dpr,lewis2020rag,izacard2021leveraging}, while explanation and calibration studies show that risk outputs should be inspectable as well as accurate \cite{ribeiro2016lime,lundberg2017shap,guidotti2018survey,arrieta2020explainable,guo2017calibration,niculescu2005predicting,zadrozny2002transforming}. However, financial risk assessment needs these elements to be combined under a strict time-validity constraint.

This paper presents the \textbf{Financial Risk Assessment Agent (FRAA)}, a retrieval-augmented model for explainable financial risk assessment. The term ``agent'' is used in a constrained operational sense: FRAA is not an open-ended autonomous system, but a structured retrieve-fuse-score-explain pipeline. For each scoring request, FRAA constructs a time-aware user representation, retrieves timestamp-valid documents from a financial knowledge base, fuses retrieved evidence with behavioral features through cross-attention, predicts a calibrated risk probability, and generates a concise explanation grounded in retrieved context and influential feature groups. The central insight is that retrieval is most useful for risk scoring when it is both time-valid and explicitly connected to the model's explanation path.

The main contributions are:
\begin{itemize}
    \item We propose FRAA, a retrieval-augmented framework that integrates time-aware sequence encoding, timestamp-valid evidence retrieval, cross-attention fusion, calibrated risk scoring, and explanation generation for financial risk assessment.
    \item We design the retrieval and fusion components to address two key challenges in financial risk modeling: newly emerging external evidence and the need for auditable explanations in high-risk decisions.
    \item We evaluate FRAA on a large-scale proprietary 24-month multi-source dataset under a strict chronological split. The results show improvements over deployed tree-based baselines and neural sequence models, and the analysis covers ablations, feature-group occlusion, retrieval depth, scenario adaptation, explanation quality, and latency.
\end{itemize}
```

### 1.4 建议修改后中文翻译

金融机构越来越多地需要基于跨时间、跨来源的信息做出风险决策。一次用户级风险评估可能依赖近期交易、账户状态、产品持仓、宏观经济信号、监管披露和市场新闻。可靠的风险估计不仅对信贷审核和欺诈防控重要，也服务于保证金控制、合规流程和分析师分流。在这些场景中，一个有用的模型应同时提供校准良好的概率、识别需要审核的高风险案例，并暴露足够证据供人工检查。

这一需求带来三个实践挑战。第一，金融行为具有时间性和不规则性。短时间交易突增、延迟还款或余额突变，其含义会取决于发生时间。第二，风险相关证据并不限于内部特征库。新闻情绪、监管文本和宏观评论可能在模型训练后才出现，但仍会影响当前案例的含义。第三，输出必须可审计。在受监管的审核流程中，仅给出概率通常不够，分析师还需要理解哪些证据和特征组支持该评分。

现有方法只能解决其中一部分问题。树集成和梯度提升模型仍是处理异构表格特征的强业务基线，但其预测状态很大程度上受人工维护的特征库限制。神经序列模型和 Transformer 架构能够捕捉不断变化的用户行为，但纯参数化模型只有在外部证据已在训练时编码或被人工作为输入特征附加时才能使用这些证据。检索增强生成和稠密检索方法可以在推理阶段基于非参数化证据生成输出，而解释性和校准研究表明，风险输出不仅要准确，还要可检查。然而，金融风险评估需要在严格时间有效约束下将这些要素结合起来。

本文提出 \textbf{Financial Risk Assessment Agent (FRAA)}，一种用于可解释金融风险评估的检索增强模型。这里的 “agent” 采用受限的业务含义：FRAA 不是开放式自主系统，而是结构化的“检索、融合、评分、解释”流程。对于每个评分请求，FRAA 构建时间感知用户表示，从金融知识库中检索时间有效文档，通过交叉注意力将检索证据与行为特征融合，预测校准风险概率，并生成基于检索上下文和重要特征组的简洁解释。本文的核心洞察是：当检索同时满足时间有效性，并与模型解释路径显式关联时，它对风险评分最有价值。

本文主要贡献如下：

- 提出 FRAA，一个用于金融风险评估的检索增强框架，整合时间感知序列编码、时间有效证据检索、交叉注意力融合、校准风险评分和解释生成。
- 设计检索和融合组件，用于应对金融风险建模中的两个关键挑战：新出现的外部证据，以及高风险决策中对可审计解释的需求。
- 在一个大规模专有 24 个月多源数据集上，采用严格时间划分评估 FRAA。结果显示，FRAA 优于已部署树模型基线和神经序列模型；分析部分覆盖消融、特征组遮蔽、检索深度、场景迁移、解释质量和延迟。

## 2. Related Work

### 2.1 修改前英文原文

```latex
\section{Related Work}

\subsection{Financial Risk Modeling and Tabular Baselines}
Credit and financial risk prediction has long relied on statistical classifiers, scorecards, tree ensembles, support vector machines, and gradient boosting. These methods handle heterogeneous tabular features and have strong empirical performance across many credit-scoring studies \cite{hand1997statistical,thomas2000survey,baesens2003benchmarking,abdou2011credit,louzada2016classification,breiman2001random,friedman2001greedy,chen2016xgboost,lessmann2015benchmarking}. Prior work also shows that credit and fraud datasets are often imbalanced and operationally asymmetric, so ranking quality and calibration matter in addition to raw classification accuracy \cite{crook2007recent,brown2012experimental,khandani2010consumer,bellotti2009support,bolton2002statistical,ngai2011application}. In production, these models are commonly supported by feature stores and domain-specific aggregation rules. Their limitation is not weak performance on static tables, but the maintenance cost of hand-crafted features and the difficulty of using newly emerging external evidence at inference time.

\subsection{Sequential User Modeling}
Sequential recommendation and behavior modeling show that recurrent networks and Transformer encoders can capture evolving user states \cite{hochreiter1997lstm,cho2014learning,vaswani2017attention,kang2018sasrec,sun2019bert4rec}. Financial risk assessment has a related temporal structure. Recent transactions, account changes, and market signals form irregular sequences whose relevance depends on timing and context. Sequence models have also been applied to credit-card fraud detection, where temporal order is central to identifying abnormal behavior \cite{jurgovsky2018sequence}. FRAA follows this line of work but adapts it to calibrated risk scoring, where explanation is part of the target behavior rather than an optional post-processing step.

\subsection{Retrieval-Augmented and Explainable AI}
Dense retrieval and RAG methods condition model outputs on non-parametric external evidence \cite{robertson2009probabilistic,karpukhin2020dpr,lewis2020rag,izacard2021leveraging}. Text encoders such as BERT and Sentence-BERT provide practical document representations for semantic matching \cite{devlin2019bert,reimers2019sentence}. Approximate nearest-neighbor methods and vector search libraries such as Facebook AI Similarity Search (FAISS) make such retrieval feasible at large scale \cite{malkov2020hnsw,johnson2019billion}. In regulated domains, however, retrieval alone is not sufficient. Local explanation methods such as LIME and SHAP \cite{ribeiro2016lime,lundberg2017shap}, broader XAI surveys \cite{guidotti2018survey,arrieta2020explainable}, and calibration analyses \cite{guo2017calibration,niculescu2005predicting,zadrozny2002transforming} all point to the same requirement: a risk model should expose not only a probability, but also the evidence used to support it.
```

### 2.2 修改前中文翻译

#### 金融风险建模与表格基线

信贷和金融风险预测长期依赖统计分类器、评分卡、树集成、支持向量机和梯度提升。这些方法能够处理异构表格特征，并在许多信贷评分研究中表现出很强的经验性能。已有研究还表明，信贷和欺诈数据集通常存在类别不平衡和业务代价不对称，因此除了原始分类准确率外，排序质量和概率校准也很重要。在生产环境中，这些模型通常由特征库和领域特定聚合规则支持。它们的局限不在于静态表格上的性能弱，而在于手工特征维护成本高，并且难以在推理时使用新出现的外部证据。

#### 序列用户建模

序列推荐和行为建模研究表明，循环网络和 Transformer 编码器能够捕捉不断变化的用户状态。金融风险评估具有相关的时间结构。近期交易、账户变化和市场信号构成不规则序列，其相关性取决于时间和上下文。序列模型也被用于信用卡欺诈检测，在该任务中，时间顺序是识别异常行为的关键。FRAA 延续这一研究方向，但将其适配到校准风险评分场景，其中解释是目标行为的一部分，而不是可选的后处理。

#### 检索增强与可解释 AI

稠密检索和 RAG 方法使模型输出能够基于非参数化外部证据。BERT 和 Sentence-BERT 等文本编码器为语义匹配提供了实用的文档表示。近似最近邻方法和 FAISS 等向量搜索库使大规模检索变得可行。然而，在受监管领域，仅有检索还不够。LIME、SHAP 等局部解释方法，更广泛的 XAI 综述，以及校准分析都指向同一需求：风险模型不仅应暴露概率，还应暴露支持该概率的证据。

### 2.3 建议修改后英文稿

```latex
\section{Related Work}

\subsection{Financial Risk Modeling and Tabular Baselines}
Credit and financial risk prediction has long relied on statistical classifiers, scorecards, tree ensembles, support vector machines, and gradient boosting. These methods handle heterogeneous tabular features and have strong empirical performance across many credit-scoring studies \cite{hand1997statistical,thomas2000survey,baesens2003benchmarking,abdou2011credit,louzada2016classification,breiman2001random,friedman2001greedy,chen2016xgboost,lessmann2015benchmarking}. Prior work also shows that credit and fraud datasets are often imbalanced and operationally asymmetric, so ranking quality and calibration matter in addition to raw classification accuracy \cite{crook2007recent,brown2012experimental,khandani2010consumer,bellotti2009support,bolton2002statistical,ngai2011application}. In production, these models are commonly supported by feature stores and domain-specific aggregation rules. Their limitation is therefore not weak performance on static tables, but the cost of maintaining hand-crafted features and the difficulty of using newly emerging external evidence at inference time. Unlike these tabular baselines, FRAA treats external evidence as a retrievable, time-constrained source rather than as a fixed set of manually attached features.

\subsection{Sequential User Modeling}
Sequential recommendation and behavior modeling show that recurrent networks and Transformer encoders can capture evolving user states \cite{hochreiter1997lstm,cho2014learning,vaswani2017attention,kang2018sasrec,sun2019bert4rec}. Financial risk assessment has a related temporal structure: recent transactions, account changes, and market signals form irregular sequences whose relevance depends on timing and context. Sequence models have also been applied to credit-card fraud detection, where temporal order is central to identifying abnormal behavior \cite{jurgovsky2018sequence}. These studies motivate the time-aware encoder in FRAA. Different from general sequential modeling, however, FRAA couples temporal behavior with timestamp-valid retrieval and explanation generation, because high-risk financial decisions require both prediction and auditability.

\subsection{Retrieval-Augmented and Explainable AI}
Dense retrieval and RAG methods condition model outputs on non-parametric external evidence \cite{robertson2009probabilistic,karpukhin2020dpr,lewis2020rag,izacard2021leveraging}. Text encoders such as BERT and Sentence-BERT provide practical document representations for semantic matching \cite{devlin2019bert,reimers2019sentence}. Approximate nearest-neighbor methods and vector search libraries such as Facebook AI Similarity Search (FAISS) make such retrieval feasible at large scale \cite{malkov2020hnsw,johnson2019billion}. In regulated domains, however, retrieval alone is not sufficient. Local explanation methods such as LIME and SHAP \cite{ribeiro2016lime,lundberg2017shap}, broader XAI surveys \cite{guidotti2018survey,arrieta2020explainable}, and calibration analyses \cite{guo2017calibration,niculescu2005predicting,zadrozny2002transforming} all point to the same requirement: a risk model should expose not only a probability, but also the evidence used to support it. FRAA differs from generic RAG systems by enforcing timestamp validity during retrieval and by tying retrieved evidence to both the scoring head and the generated explanation.
```

### 2.4 建议修改后中文翻译

#### 金融风险建模与表格基线

信贷和金融风险预测长期依赖统计分类器、评分卡、树集成、支持向量机和梯度提升。这些方法能够处理异构表格特征，并在许多信贷评分研究中表现出很强的经验性能。已有研究还表明，信贷和欺诈数据集通常存在类别不平衡和业务代价不对称，因此除了原始分类准确率外，排序质量和概率校准也很重要。在生产环境中，这些模型通常由特征库和领域特定聚合规则支持。因此，它们的局限不在于静态表格上的性能弱，而在于手工特征维护成本高，并且难以在推理时使用新出现的外部证据。不同于这些表格基线，FRAA 将外部证据视为可检索且受时间约束的信息来源，而不是固定的一组人工附加特征。

#### 序列用户建模

序列推荐和行为建模研究表明，循环网络和 Transformer 编码器能够捕捉不断变化的用户状态。金融风险评估具有相关的时间结构：近期交易、账户变化和市场信号构成不规则序列，其相关性取决于时间和上下文。序列模型也被用于信用卡欺诈检测，在该任务中，时间顺序是识别异常行为的关键。这些研究为 FRAA 的时间感知编码器提供了动机。然而，不同于一般序列建模，FRAA 将时间行为与时间有效检索和解释生成结合起来，因为高风险金融决策同时需要预测和可审计性。

#### 检索增强与可解释 AI

稠密检索和 RAG 方法使模型输出能够基于非参数化外部证据。BERT 和 Sentence-BERT 等文本编码器为语义匹配提供了实用的文档表示。近似最近邻方法和 FAISS 等向量搜索库使大规模检索变得可行。然而，在受监管领域，仅有检索还不够。LIME、SHAP 等局部解释方法，更广泛的 XAI 综述，以及校准分析都指向同一需求：风险模型不仅应暴露概率，还应暴露支持该概率的证据。FRAA 不同于通用 RAG 系统之处在于，它在检索阶段强制执行时间有效性，并将检索证据同时关联到评分头和生成解释。

## 3. Limitations and Reproducibility

### 3.1 修改前英文原文

```latex
\section{Limitations and Reproducibility}

The evaluation uses a proprietary financial dataset that cannot be publicly released because it contains sensitive user-level risk and transaction information. This limits independent replication. To support reproducibility within this constraint, we report the data scale, feature families, chronological split, training configuration, retrieval depth, baselines, and ablation settings. The knowledge base is timestamped, and retrieval uses only documents available at or before the scoring time. These details make the experimental protocol auditable even when the raw records cannot be shared.

Several claims should be read within this scope. First, the results establish performance in one large multi-source financial environment, not universal transfer across institutions. Second, the business-utility analysis is based on offline replay and should not be interpreted as causal online impact evidence. Third, the current evaluation does not report multi-seed variance or formal statistical significance tests because the available audit record contains one fixed proprietary chronological split rather than repeated independent training runs. Fourth, the explanation evaluation measures expert-perceived faithfulness and readability for high-risk test predictions. It does not remove the need for human oversight in regulated decisions. Future work should add public benchmark evaluations where legally possible, repeatability analysis under multiple splits or random seeds, institution-level transfer tests, and governance mechanisms for retrieved evidence that may become outdated or disputed.
```

### 3.2 修改前中文翻译

本文评估使用的是专有金融数据集，由于包含敏感的用户级风险和交易信息，无法公开发布。这限制了独立复现。为在这一限制下支持可复现性，本文报告了数据规模、特征族、时间划分、训练配置、检索深度、基线和消融设置。知识库带有时间戳，检索仅使用评分时间之前或评分时间当时可用的文档。即使原始记录无法共享，这些细节也使实验协议具有可审计性。

若干结论应在上述范围内理解。第一，结果证明了方法在一个大型多源金融环境中的表现，而不是跨机构的普遍迁移能力。第二，业务效用分析基于离线回放，不应被解读为因果性的线上收益证据。第三，当前评估未报告多随机种子方差或正式统计显著性检验，因为可用审计记录只包含一个固定的专有时间划分，而不是多次独立训练。第四，解释评估衡量的是领域专家对高风险测试预测的感知忠实度和可读性，并不意味着在受监管决策中可以取消人工监督。未来工作应在法律允许的情况下增加公开基准评估、多划分或多随机种子的可重复性分析、机构级迁移测试，以及对可能过时或存在争议的检索证据的治理机制。

### 3.3 建议修改后英文稿

```latex
\section{Limitations and Reproducibility}

The evaluation uses a proprietary financial dataset that cannot be publicly released because it contains sensitive user-level risk and transaction information. This limits independent replication. To support reproducibility within this constraint, we report the data scale, feature families, chronological split, training configuration, retrieval depth, baselines, and ablation settings. The knowledge base is timestamped, and retrieval uses only documents available at or before the scoring time. These details make the experimental protocol auditable even when the raw records cannot be shared.

Several empirical claims should be read within this scope. First, the results establish performance in one large multi-source financial environment, not universal transfer across institutions. Second, the business-utility analysis is based on offline replay and should not be interpreted as causal online impact evidence. Third, the current evaluation does not report multi-seed variance or formal statistical significance tests because the available audit record contains one fixed proprietary chronological split rather than repeated independent training runs. Fourth, the explanation evaluation measures expert-perceived faithfulness and readability for high-risk test predictions. It does not remove the need for human oversight in regulated decisions.

The deployment of retrieval-augmented risk models also raises governance issues. Retrieved documents may become outdated, incomplete, or disputed, and the model may inherit biases present in historical risk labels, product access patterns, or external financial text. Although FRAA constrains retrieval by timestamp and exposes evidence in generated explanations, these mechanisms do not by themselves guarantee fairness, privacy, or safety. In high-stakes financial use, the model should therefore be used as decision support rather than as an automatic decision maker, with additional controls for data protection, bias monitoring, evidence auditing, and appeal or review procedures.

Future work should add public benchmark evaluations where legally possible, repeatability analysis under multiple splits or random seeds, institution-level transfer tests, and governance mechanisms for retrieved evidence that may become outdated or disputed. A further direction is to study lightweight deployment and uncertainty-aware explanation, so that retrieval-augmented risk scoring can be made both more efficient and more transparent under operational constraints.
```

### 3.4 建议修改后中文翻译

本文评估使用的是专有金融数据集，由于包含敏感的用户级风险和交易信息，无法公开发布。这限制了独立复现。为在这一限制下支持可复现性，本文报告了数据规模、特征族、时间划分、训练配置、检索深度、基线和消融设置。知识库带有时间戳，检索仅使用评分时间之前或评分时间当时可用的文档。即使原始记录无法共享，这些细节也使实验协议具有可审计性。

若干经验性结论应在上述范围内理解。第一，结果证明了方法在一个大型多源金融环境中的表现，而不是跨机构的普遍迁移能力。第二，业务效用分析基于离线回放，不应被解读为因果性的线上收益证据。第三，当前评估未报告多随机种子方差或正式统计显著性检验，因为可用审计记录只包含一个固定的专有时间划分，而不是多次独立训练。第四，解释评估衡量的是领域专家对高风险测试预测的感知忠实度和可读性，并不意味着在受监管决策中可以取消人工监督。

检索增强风险模型的部署还会带来治理问题。检索到的文档可能会过时、不完整或存在争议，模型也可能继承历史风险标签、产品访问模式或外部金融文本中的偏差。尽管 FRAA 通过时间戳约束检索，并在生成解释中暴露证据，但这些机制本身并不能保证公平性、隐私或安全性。因此，在高风险金融场景中，该模型应作为决策支持工具，而不是自动决策者，并需要额外的数据保护、偏差监控、证据审计以及申诉或复核流程。

未来工作应在法律允许的情况下增加公开基准评估、多划分或多随机种子的可重复性分析、机构级迁移测试，以及对可能过时或存在争议的检索证据的治理机制。另一个方向是研究轻量化部署和不确定性感知解释，使检索增强风险评分在业务约束下同时更加高效和透明。

## 4. 使用建议

- 该文件中的“建议修改后英文稿”可作为后续替换 TeX 对应章节的候选版本。
- 当前文件不自动修改投稿源文件；如需正式采用，应再检查篇幅、浮动体位置和 LNCS 页数。
- 若后续替换到 TeX，建议同步检查引用、页数、Overleaf 编译日志和 PDF 排版。
